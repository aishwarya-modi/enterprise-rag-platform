# Infrastructure

This folder is intended for infrastructure-as-code assets such as Terraform, environment definitions, and provisioning scripts.

It exists to separate cloud and platform setup from application code so systems can be provisioned consistently and safely.
