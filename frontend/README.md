# Frontend

This folder contains the client-facing application and its presentation layer.

It exists to isolate UI development from backend services and deployment concerns while keeping the user experience maintainable and testable.
