# Kubernetes Manifests

This folder holds Kubernetes manifests and deployment resources.

It exists to keep runtime configuration explicit and environment-aware while supporting direct cluster deployment when Helm is not used.
