# Docker

This folder is reserved for container-related resources such as Dockerfiles, image build helpers, and local development container definitions.

It exists to standardize packaging and make the application portable across environments.
